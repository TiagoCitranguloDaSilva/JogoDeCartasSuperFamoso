// Variáveis
// Essa é a mão do usuario
var UserHand = document.getElementById("mao");

// Essa é a mão do usuario
var OpponentHand = document.getElementById("ad");

// Essa é a area das cartas
var TableCards = document.getElementById("cartas");

// Essa é a area das cartas
var GetCard = document.getElementById("pescar");

var Music = document.getElementById("Music");

// As possiveis cartas atualmente
var cartasPossiveis = [];

var vez = "User";